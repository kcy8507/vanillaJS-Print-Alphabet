function printAlphabet(x) {
  let alphabet = [...x];
  console.log(alphabet);
  console.log(`입력한 배열: ${alphabet}`);

  alphabet.forEach((el, i) => {});
  console.log(alphabet);
}
printAlphabet("aabc");
